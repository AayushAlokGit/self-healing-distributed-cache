# What NOT to send to the build. The image ships one Go binary; none of this compiles it.
# This file is what lets the Dockerfile say `COPY . .` instead of naming each package.
.git/
.gitignore
.dockerignore
Dockerfile
render.yaml
README.md
CLAUDE.md

frontend/
docs/
logs/
bin/
